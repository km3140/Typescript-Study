{
  "include": ["src"]
  // 👆 ts파일이 들어있는 위치
  "compilerOptions":{
    "outDir": "build",
    // 👆 컴파일된 js파일이 생성되는 위치
    "target": "ES6",
    // 👆 컴파일 할 js의 버전, ES6를 사용하는 것이 가장 이상적
    "lib" : ["ES6","DOM"]
    // 👆 해당 ts파일이 실행될 환경을 명시?
    //    DOM은 브라우저에서 실행되는 코드라는 것을 뜻함 -> ts에서 브라우저api의 자동완성 기능을 제공함
    //    js의 기본 type definition은 ts가 갖고있다
  }
}

npm run tsc

현재 프로젝트에서는 package.json에 tsc의 alias로 build를 주었음
=> npm run build
